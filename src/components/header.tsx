"use client";

import React from "react";
import NavBar from "./navbar";


const Header: React.FC = () => {
    return (
        <header>
            <NavBar />
        </header>
    );
};

export default Header;
